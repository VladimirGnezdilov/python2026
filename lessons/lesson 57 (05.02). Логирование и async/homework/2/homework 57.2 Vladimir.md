

# 📧 Homework 57.2: Асинхронный почтовый сервис

## Описание задачи

Написать программу, которая имитирует работу почтового сервиса. Сервис должен асинхронно отправлять письма разным пользователям с разными задержками.

---

## 📋 Указания

### 1. Создайте функцию `send_email(recipient, delay)`

Функция должна принимать:
- `recipient` — имя получателя
- `delay` — задержку в секундах

**Что должна делать функция:**
- Выводить сообщение о начале отправки письма
- Использовать `await asyncio.sleep(delay)` для имитации времени отправки
- Выводить сообщение о завершении отправки письма

### 2. Основная функция программы

1. Создайте список пользователей и задержек отправки:
   ```python
   users = [("Alice", 2), ("Bob", 3), ("Charlie", 1), ("Diana", 4)]
   ```

2. Для каждого пользователя создайте асинхронную задачу с помощью `asyncio.create_task`

3. Ожидайте завершения всех задач

4. Используйте `asyncio.run()` для запуска программы

---

## 🎯 Ожидаемый результат

Программа должна конкурентно отправлять письма, и сообщения о начале и завершении отправки будут появляться в случайном порядке, соответствующем задержкам.

---

## 💡 Пример реализации (псевдокод)

```
ФУНКЦИЯ отправить_письмо(получатель, задержка):
    ВЫВЕСТИ "Начинаю отправку письма для " + получатель
    ПРИОСТАНОВИТЬ_ВЫПОЛНЕНИЕ(задержка)
    ВЫВЕСТИ "Письмо для " + получатель + " отправлено!"

ФУНКЦИЯ главная():
    пользователи = [
        ("Alice", 2),
        ("Bob", 3), 
        ("Charlie", 1),
        ("Diana", 4)
    ]
    
    задачи = СОЗДАТЬ_ЗАДАЧУ(для каждого (пользователь, задержка) в пользователи):
        отправить_письмо(пользователь, задержка)
    
    ДЛЯ каждой задачи в задачи:
        ДОЖДАТЬСЯ_ЗАДАЧИ(задача)

ЗАПУСТИТЬ_ПРОГРАММУ(главная)
```

---

## 🔍 Детальный разбор задачи

### Шаг 1: Понимание асинхронности
В отличие от синхронного выполнения, где задачи выполняются последовательно, асинхронное выполнение позволяет запускать несколько задач и переключаться между ними во время ожидания.

### Шаг 2: Структура программы
1. **Функция отправки письма** — асинхронная функция, которая имитирует длительную операцию
2. **Главная функция** — координирует все задачи отправки
3. **Запуск программы** — точка входа в асинхронную программу

### Шаг 3: Конвертация в реальный код

| Псевдокод | Python эквивалент | Объяснение |
|-----------|-------------------|------------|
| `ФУНКЦИЯ отправить_письмо` | `async def send_email` | `async` указывает, что функция асинхронная |
| `ПРИОСТАНОВИТЬ_ВЫПОЛНЕНИЕ` | `await asyncio.sleep` | `await` приостанавливает выполнение функции без блокировки всей программы |
| `СОЗДАТЬ_ЗАДАЧУ` | `asyncio.create_task` | Создает задачу, которая будет выполняться concurrently |
| `ДОЖДАТЬСЯ_ЗАДАЧИ` | `await task` | Ждет завершения конкретной задачи |
| `ЗАПУСТИТЬ_ПРОГРАММУ` | `asyncio.run` | Запускает асинхронную программу |

### Шаг 4: Ключевые концепции

1. **Event Loop (Цикл событий)** — ядро асинхронной программы, которое управляет выполнением задач
2. **Coroutines (Корутины)** — функции, которые можно приостанавливать и возобновлять
3. **Tasks (Задачи)** — обертки над корутинами, которые планируются для выполнения в event loop
4. **Await** — оператор, который приостанавливает выполнение корутины до завершения другой асинхронной операции

### Шаг 5: Порядок выполнения
1. Программа запускается через `asyncio.run`
2. Создаются все задачи отправки писем
3. Каждая задача начинает выполняться и выводит сообщение о начале
4. Когда задача доходит до `await asyncio.sleep`, она "отдает управление" циклу событий
5. Цикл событий переключается на другую готовую задачу
6. По завершении задержки каждая задача продолжает выполнение и выводит сообщение об отправке
