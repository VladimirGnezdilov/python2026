Вопросы с выбором варианта: 78/78%  
Вопросы с текстовыми ответами: 22/22%  
# Вопрос 1. Блок 6. Flexbox: выравнивание и простая сетка
  
### Содержание и вопросы

- Что такое flex-контейнер и flex-элемент?
- Чем отличаются `justify-content` и `align-items`?
- Что делает `gap`?
- Как сделать перенос карточек?

### Материал (лаконично)

- Flexbox это способ разложить элементы в ряд или в колонку и управлять выравниванием.
- Когда ты включаешь `display: flex`, дети внутри становятся flex-элементами.
- `justify-content` это распределение по главной оси. Обычно это горизонталь.
- `align-items` это выравнивание по другой оси. Обычно это вертикаль.
- `gap` это расстояние между элементами, без “костылей” с `margin`.
- `flex-wrap` позволяет элементам переноситься, если места не хватает.
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Это не вопрос на проверку знаний, а подтверждение ознакомления с материалом.  Выбор "Прочитано" означает, что студент изучил предоставленную теорию по основам Flexbox.  Для успешного выполнения практического задания важно понимать описанные концепции: контейнеры, оси, выравнивание и управление переносом.  
#  Вопрос 2. Практическое задание (Блок 6)
  
### Условия

Сделай flex-верстку пошагово:

1. В HTML оберни содержимое шапки в отдельный блок или назначь класс шапке.
2. В `styles.css` включи для шапки `display: flex`.
3. Настрой выравнивание, чтобы:
   - аватар и текст стояли в одну строку
   - элементы были аккуратно выровнены
4. Добавь расстояние между аватаром и текстом.
5. Для секции проектов:
   - сделай контейнер карточек flex-контейнером
   - включи перенос
   - добавь промежутки
6. Настрой правило ширины карточек так, чтобы на широком экране было несколько карточек в ряд.
7. Сожми окно браузера и проверь, что карточки переносятся, а страница не ломается.

Ограничения:

- Сетка должна адекватно реагировать на сужение окна.

### Псевдокод решения

```text
IN CSS_FILE
  MAKE HEADER_ROW_LAYOUT
    SET DISPLAY_FLEX
    SET ALIGNMENT
    SET GAP

  MAKE CARDS_GRID
    SET DISPLAY_FLEX
    ENABLE WRAP
    SET GAP

  FOR EACH CARD
    SET FLEX_BASIS_OR_WIDTH_RULE
END
```

### Если использовал ИИ, обязательные изменения вручную

- Измени поведение сетки на узком экране (например, чтобы карточка становилась на всю ширину).
- Добавь выравнивание элементов в шапке так, чтобы визуально было ровно.

---

**Вставь решение в комментарий ниже:**
  
### 🟢 Ответ
.header {  
  display: flex;  
  align-items: center;  
  gap: 1rem;  
}  
  
.projects-grid {  
  display: flex;  
  flex-wrap: wrap;  
  gap: 1.5rem;  
}  
  
.card {  
  flex: 1 1 280px;  
}  
  
### ИИ анализ
Отличное решение! Код точно соответствует всем пунктам задания: созданы flex-контейнеры для шапки и сетки, настроено выравнивание и добавлены промежутки.  Использование `flex: 1 1 280px;` для карточек — это правильный подход, который обеспечивает адаптивность и несколько карточек в ряд на широких экранах.  Для дальнейшего улучшения можно поэкспериментировать с медиазапросами, чтобы точнее контролировать поведение на очень узких экранах.  
# Вопрос 3. Что происходит с дочерними элементами, если у родительского контейнера стоит `display: flex`?
  
  
Варианты ответов:
1) Они удаляются из страницы
2) Они отключают CSS
3) ✅ Они становятся flex-элементами
4) Они становятся `meta` тегами
5) Они превращаются в картинки
  
### ИИ анализ
Правильный ответ выбран.  Ключевая концепция Flexbox заключается в том, что при применении `display: flex` к контейнеру его непосредственные дочерние элементы автоматически становятся flex-элементами.  Это меняет модель их расположения, позволяя управлять выравниванием, порядком и размерами вдоль главной и поперечной осей.  Понимание этой связи "контейнер-элемент" фундаментально для работы с Flexbox.  
# Вопрос 4. За что отвечает свойство `justify-content` в flexbox (распределение по главной оси)?
  
  
Варианты ответов:
1) ✅ За распределение по главной оси
2) За видимость картинки
3) За цвет фона
4) За тип списка
5) За размер шрифта
  
### ИИ анализ
Ответ верный.  Свойство `justify-content` управляет распределением flex-элементов вдоль главной оси контейнера (по умолчанию — горизонтальной).  Оно позволяет выравнивать элементы в начале, центре, конце контейнера, равномерно распределять их с отступами или без.  Это основное свойство для управления пространством по основной линии направления flex-контейнера.  
# Вопрос 5. За что отвечает свойство `align-items` в flexbox (выравнивание по поперечной оси)?
  
  
Варианты ответов:
1) ✅ За выравнивание по другой оси
2) За язык страницы
3) За высоту контейнера
4) За количество элементов
5) За тип ссылки
  
### ИИ анализ
Ответ правильный.  Свойство `align-items` контролирует выравнивание элементов по поперечной оси (перпендикулярной главной), которая по умолчанию является вертикальной.  Оно определяет, как элементы располагаются внутри контейнера по высоте: растягиваются, выравниваются по верхнему или нижнему краю, или по базовой линии текста.  Вместе с `justify-content` оно обеспечивает полный контроль над выравниванием.  
# Вопрос 6. Что делает свойство `gap` в flexbox-контейнере?
  
  
Варианты ответов:
1) Добавляет тень
2) Увеличивает размер шрифта
3) ✅ Добавляет расстояние между элементами
4) Меняет цвет рамки
5) Скрывает элементы
  
### ИИ анализ
Верный ответ.  Свойство `gap` (или `row-gap`/`column-gap`) задает фиксированное расстояние между соседними flex-элементами как по строке, так и по колонке.  Это современная и удобная альтернатива использованию полей (`margin`) для создания отступов между элементами в сетке, так как `gap` добавляет промежуток только *между* элементами, не влияя на внешние отступы.  
# Вопрос 7. Какое свойство flexbox позволяет элементам переноситься на новую строку, если не хватает места?
  
  
Варианты ответов:
1) ✅ `flex-wrap: wrap`
2) `float: left`
3) `text-align: center`
4) `position: absolute`
5) `display: block`
  
### ИИ анализ
Ответ правильный.  По умолчанию flex-элементы пытаются разместиться в одну строку (ось).  Свойство `flex-wrap: wrap` отменяет это поведение, позволяя элементам переноситься на следующую строку (или колонку), когда они не помещаются в контейнере.  Это ключевое свойство для создания адаптивных flex-сеток, которые корректно отображаются на экранах разной ширины.  
# Вопрос 8. Что нужно проверить после настройки flexbox-сетки для карточек проектов?
  
  
Варианты ответов:
1) Что все отступы нулевые
2) Что все заголовки одинаковые
3) ✅ Что карточки переносятся при сужении
4) Что все ссылки ведут на один сайт
5) Что все картинки одинаковые
  
### ИИ анализ
Правильный ответ выбран.  Основная цель создания адаптивной flex-сетки — обеспечить корректное отображение при изменении размера окна.  Поэтому критически важно проверить, что при сужении окна браузера карточки действительно переносятся на новую строку, а не выходят за границы контейнера или не создают горизонтальную прокрутку.  Это проверка работоспособности `flex-wrap` и гибких размеров элементов.  
# Вопрос 9. Зачем настраивать ширину карточек во flexbox-контейнере?
  
  
Варианты ответов:
1) ✅ Чтобы было несколько в ряд на широком экране
2) Чтобы убрать рамки
3) Чтобы скрыть текст
4) Чтобы все были на одной строке
5) Чтобы все были разного размера
  
### ИИ анализ
Ответ верный.  Без явного указания ширины или гибкости (`flex-basis`, `flex-grow`, `flex-shrink`) flex-элементы могут либо сжиматься до минимального размера содержимого, либо растягиваться на всю доступную ширину.  Настройка ширины (например, через `flex: 1 1 280px`) позволяет контролировать, сколько карточек поместится в ряд, создавая предсказуемую и визуально приятную сетку, которая использует доступное пространство на широких экранах.
