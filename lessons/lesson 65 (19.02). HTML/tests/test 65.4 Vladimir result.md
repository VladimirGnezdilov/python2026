Вопросы с выбором варианта: 78/78%  
# Вопрос 1. Блок 5. Box model и управление отступами
  
### Содержание и вопросы

- Что входит в box model?
- Чем `margin` отличается от `padding`?
- Зачем нужен `box-sizing: border-box`?
- Почему элемент “не влезает” в ширину?

### Материал (лаконично)

- У каждого блока есть “коробка”.
- `padding` это воздух внутри блока. Он отодвигает текст от края рамки.
- `margin` это расстояние снаружи блока. Он раздвигает блоки друг от друга.
- Если ты задаешь элементу ширину и добавляешь padding и рамку, без `border-box` элемент может стать шире, чем ты ожидал.
- `box-sizing: border-box` делает ширину предсказуемой. Padding и рамка входят в заданную ширину.
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Этот вопрос проверяет ознакомление с материалом. Поскольку отмечен вариант "Прочитано", можно сделать вывод, что студент изучил предоставленную информацию о box model. Важно, чтобы это теоретическое понимание было применено на практике в следующих заданиях.  

#  Вопрос 2. Практическое задание (Блок 5)
  
### Условия

Наведи порядок в отступах пошагово:

1. В `styles.css` включи `border-box` для всех элементов одним правилом.
2. Выбери один основной вертикальный отступ и используй его для секций.
3. Применяй этот отступ ко всем секциям одинаково.
4. Для карточек проектов:
   - добавь внутренний отступ
   - добавь рамку или фон
   - добавь скругление углов
5. Открой страницу и проверь:
   - текст не прилипает к краям
   - расстояния между секциями выглядят одинаково

Ограничения:

- Отступы должны быть согласованными. Не делай “случайные” значения для каждого элемента.

### Псевдокод решения

```text
IN CSS_FILE
  APPLY BORDER_BOX_TO_ALL

  FOR EACH SECTION_BLOCK
    SET OUTER_SPACING

  FOR EACH CARD_BLOCK
    SET INNER_SPACING
    SET BORDER_OR_BG
    SET CORNER_ROUNDING
END
```

### Если использовал ИИ, обязательные изменения вручную

- Перестрой отступы так, чтобы они задавались через 1-2 “основных” правила, а не через множество точечных.
- Добавь правило, которое улучшает поведение изображений в карточках (без фиксированной ширины).

---

**Вставь решение в комментарий ниже:**
  
### 🟢 Ответ
*,  
*::before,  
*::after {  
  box-sizing: border-box;  
}  
  
.section {  
  margin-bottom: 2rem;  
}  
  
.card {  
  padding: 1.5rem;  
  border: 1px solid #ddd;  
  border-radius: 0.5rem;  
}  
  
.card img {  
  max-width: 100%;  
  height: auto;  
  display: block;  
}  
  
### ИИ анализ
Решение отличное. Студент корректно применил `border-box` для всех элементов, задал согласованный вертикальный отступ для секций и оформил карточки с внутренним отступом, рамкой и скруглением. Правило для изображений (`max-width: 100%`) идеально предотвращает выход картинки за границы блока, делая верстку адаптивной. Все требования задания выполнены полностью.  

# Вопрос 3. Что такое `padding` в CSS простыми словами (отступ внутри блока)?
  
  
Варианты ответов:
1) Отступ между блоками
2) Цвет рамки
3) ✅ Отступ внутри блока
4) Ссылка на файл
5) Тип шрифта
  
### ИИ анализ
Ответ верный. `padding` — это внутренний отступ элемента, который создает пространство между его содержимым (например, текстом) и границей (`border`). Это ключевая часть box model, влияющая на внутренние размеры блока. Понимание разницы между `padding` и `margin` (внешний отступ) критически важно для верстки.  

# Вопрос 4. Что такое `margin` в CSS простыми словами (отступ снаружи блока)?
  
  
Варианты ответов:
1) Размер картинки
2) Название секции
3) Отступ внутри блока
4) Высота текста
5) ✅ Отступ снаружи блока
  
### ИИ анализ
Ответ правильный. `margin` определяет внешние отступы элемента, создавая пространство между этим блоком и соседними элементами. В отличие от `padding`, `margin` не влияет на фон или внутреннее содержимое блока, а только на его положение относительно других. Это основной инструмент для управления компоновкой и расстояниями на странице.  

# Вопрос 5. Для чего обычно используют свойство `box-sizing: border-box` в CSS?
  
  
Варианты ответов:
1) Чтобы отключить фон
2) Чтобы увеличить шрифт
3) ✅ Чтобы ширина была предсказуемой
4) Чтобы скрыть ссылки
5) Чтобы заменить flexbox
  
### ИИ анализ
Ответ верный. Свойство `box-sizing: border-box` изменяет модель расчета размеров элемента: заданная ширина (`width`) начинает включать в себя `padding` и `border`. Это предотвращает ситуацию, когда добавление внутренних отступов или рамки неожиданно увеличивает общую ширину элемента, что часто приводит к появлению горизонтальной прокрутки.  

# Вопрос 6. Почему на странице может появиться горизонтальная полоса прокрутки из-за размеров элементов?
  
  
Варианты ответов:
1) `title` слишком длинный
2) ✅ Элемент стал шире экрана
3) `footer` пустой
4) `meta` не тот
5) `head` слишком большой
  
### ИИ анализ
Ответ правильный. Горизонтальная прокрутка чаще всего появляется, когда суммарная ширина какого-либо элемента (с учетом его `width`, `padding`, `border` и `margin`) превышает ширину области просмотра (viewport). Использование `box-sizing: border-box` и контроль максимальной ширины (`max-width: 100%`) для гибких элементов помогают избежать этой проблемы.  

# Вопрос 7. Что значит “согласованные отступы” при работе с CSS?
  
  
Варианты ответов:
1) Отступов нет нигде
2) Отступы только у картинок
3) Каждый блок со своим числом
4) Отступы только у ссылок
5) ✅ Одни и те же правила повторяются
  
### ИИ анализ
Ответ верный. "Согласованные отступы" означают использование единой, логической системы отступов (например, одного или двух базовых значений) для однотипных элементов на всей странице. Это создает визуальную гармонию, упрощает поддержку кода и делает интерфейс более профессиональным, в отличие от задания случайных, разных значений для каждого элемента.  

# Вопрос 8. Что из перечисленного относится к CSS box model (модель блока)?
  
  
Варианты ответов:
1) ul, ol, li, a
2) hover, active, visited, focus
3) ✅ margin, border, padding, content
4) jpg, png, svg, gif
5) title, meta, link, script
  
### ИИ анализ
Ответ правильный. CSS Box Model — это фундаментальная концепция, описывающая прямоугольные блоки, генерируемые для элементов. Она включает в себя, изнутри наружу: область содержимого (`content`), внутренний отступ (`padding`), границу (`border`) и внешний отступ (`margin`). Понимание этой модели необходимо для точного контроля размеров и расположения элементов.  

# Вопрос 9. Какое CSS-правило для изображений в карточках помогает избежать проблем с шириной блока?
  
  
Варианты ответов:
1) Картинка становится ссылкой
2) ✅ Картинка не ломает ширину блока
3) Картинка отключает рамку
4) Картинка добавляет заголовок
5) Картинка меняет язык страницы
  
### ИИ анализ
Ответ верный, хотя сформулирован как результат, а не как правило. Подразумевается использование правил вроде `max-width: 100%` и `height: auto`. Они гарантируют, что изображение будет масштабироваться внутри родительского контейнера, не превышая его ширину и сохраняя пропорции. Это ключевой прием для создания адаптивных и "неломающихся" карточек.  

Вопросы с текстовыми ответами: 100/22%
