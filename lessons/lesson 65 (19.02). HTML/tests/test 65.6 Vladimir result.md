Вопросы с выбором варианта: 78/78%  
# Вопрос 1. Блок 7. Финальная полировка: состояния ссылок, типографика, адаптация
  
### Содержание и вопросы

- Что такое `:hover` и зачем он нужен?
- Какие базовые типографические настройки делают страницу читабельной?
- Как избежать горизонтального скролла?
- Как проверить адаптацию без “сложной” верстки?

### Материал (лаконично)

- `:hover` это стиль, который включается при наведении мыши. Пользователь видит, что элемент кликабельный.
- Типографика это удобство чтения:
  - нормальная высота строк
  - понятные размеры заголовков
  - достаточные отступы
- Адаптация на базовом уровне это когда страница не разваливается на узком экране.
- Частая ошибка новичка это фиксированная ширина у блоков или картинок, из-за которой появляется горизонтальный скролл.
  
Варианты ответов:
1) ✅ Прочитано
  
### ИИ анализ
Ответ отмечен как "Прочитано", что означает ознакомление с материалом.  Для успешного применения знаний важно не только прочитать теорию, но и выполнить практические шаги, описанные в следующем вопросе, чтобы закрепить понимание.  

#  Вопрос 2. Практическое задание (Блок 7)
  
### Условия

Доведи страницу до состояния “готово к показу” пошагово:

1. В `styles.css` добавь для ссылок правило `hover`, чтобы при наведении менялся внешний вид.
2. Настрой читабельность:
   - задай межстрочный интервал
   - проверь, что заголовки заметно отличаются от обычного текста
3. Открой страницу и проверь горизонтальный скролл.
4. Если скролл есть:
   - найди элемент, который шире экрана
   - исправь ширины и поведение изображений
5. Проверь изображения:
   - они должны уменьшаться на узком экране
   - они не должны выталкивать карточки
6. Сожми окно браузера до узкого размера и проверь, что все секции остаются читаемыми.

### Псевдокод решения

```text
IN CSS_FILE
  STYLE LINKS_HOVER_STATE
    CHANGE VISUAL_PROPERTY

  STYLE TYPOGRAPHY
    SET LINE_HEIGHT
    SET HEADING_SIZES

  CHECK RESPONSIVENESS
    LIMIT CONTAINER_WIDTH
    ENSURE MEDIA_SCALING
END
```

### Если использовал ИИ, обязательные изменения вручную

- Добавь еще одно интерактивное состояние (например, для кнопки или ссылки в карточке), но без JS.
- Сделай одну осознанную правку дизайна: поменяй один глобальный параметр (цвет, фон или шрифт) и проверь, что все осталось читабельным.

---

**Вставь решение в комментарий ниже:**
  
### 🟡 Ответ
*,  
*::before,  
*::after {  
  box-sizing: border-box;  
}  
  
body {  
  margin: 0;  
  font-family: 'Arial', sans-serif;  
  line-height: 1.6;  
  color: #222;  
  background-color: #fff;  
}  
  
h1, h2, h3 {  
  font-weight: 600;  
  line-height: 1.2;  
  margin-top: 0;  
}  
  
.section {  
  margin-bottom: 2rem;  
  padding: 0 1rem;  
}  
  
.header {  
  display: flex;  
  align-items: center;  
  gap: 1rem;  
  background-color: #f0f0f0;  
  padding: 1rem;  
}  
  
.projects-grid {  
  display: flex;  
  flex-wrap: wrap;  
  gap: 1.5rem;  
  justify-content: flex-start;  
}  
  
.card {  
  flex: 1 1 280px;  
  padding: 1.5rem;  
  background-color: #f9f9f9;  
  border: 1px solid #ddd;  
  border-radius: 0.5rem;  
  transition: background-color 0.2s ease, box-shadow 0.2s ease;  
}  
  
.card:hover {  
  background-color: #fff;  
  box-shadow: 0 4px 8px rgba(0,0,0,0.1);  
}  
  
.card img {  
  max-width: 100%;  
  height: auto;  
  display: block;  
}  
  
a {  
  color: #0066cc;  
  text-decoration: none;  
  transition: color 0.2s ease;  
}  
  
a:hover {  
  color: #004499;  
  text-decoration: underline;  
}  
  
a:focus {  
  outline: 2px solid #0066cc;  
  outline-offset: 2px;  
}  
  
.card a {  
  font-weight: 500;  
}  
  
.card a:hover {  
  color: #002277;  
  text-decoration: underline;  
}  
  
### ИИ анализ
Представленный CSS код демонстрирует хорошее понимание ключевых задач: настроены hover-эффекты для ссылок и карточек, задана типографика (`line-height`, заголовки) и обеспечена адаптивность изображений (`max-width: 100%`).  Однако, решение неполное: отсутствует проверка на горизонтальный скролл и явное исправление элементов с фиксированной шириной, а также не выполнено обязательное требование добавить еще одно интерактивное состояние (например, `:active` для кнопки) и осознанную глобальную правку дизайна.  

# Вопрос 3. Что такое псевдо-класс `:hover` в CSS (стиль при наведении мыши)?
  
  
Варианты ответов:
1) Стиль только для картинок
2) Стиль внутри `meta`
3) ✅ Стиль при наведении мыши
4) Стиль для `doctype`
5) Стиль внутри `head`
  
### ИИ анализ
Выбран правильный вариант.  Псевдо-класс `:hover` применяет стили к элементу, когда пользователь наводит на него курсор мыши, что является стандартным способом визуальной обратной связи для интерактивных элементов.  Это ключевой инструмент для улучшения пользовательского опыта на веб-странице.  

# Вопрос 4. Какое CSS-свойство помогает сделать текст более читабельным на веб-странице?
  
  
Варианты ответов:
1) ✅ Нормальный `line-height`
2) Текст без абзацев
3) Текст без пробелов
4) Очень мелкий шрифт
5) Текст без заголовков
  
### ИИ анализ
Ответ верный.  Свойство `line-height` (межстрочный интервал) напрямую влияет на читабельность, предотвращая слияние строк текста.  Оптимальное значение, такое как 1.5 или 1.6, значительно облегчает восприятие текстового контента.  

# Вопрос 5. Что чаще всего вызывает горизонтальный скролл на мобильных устройствах?
  
  
Варианты ответов:
1) Наличие `footer`
2) Наличие `section`
3) ✅ Элемент с фиксированной шириной
4) Наличие `main`
5) Один `h1` на странице
  
### ИИ анализ
Правильный ответ выбран.  Элементы с фиксированной шириной в пикселях (например, `width: 1200px`) не могут уменьшиться под размер узкого экрана мобильного устройства, что и приводит к появлению нежелательного горизонтального скролла.  Решение — использовать относительные единицы или `max-width`.  

# Вопрос 6. Какое поведение изображений лучше всего подходит для адаптивного дизайна?
  
  
Варианты ответов:
1) Изображение только в `head`
2) Изображение всегда фиксированной ширины
3) ✅ Изображение уменьшается под контейнер
4) Изображение без `alt` текста
5) Изображение всегда больше экрана
  
### ИИ анализ
Ответ правильный.  Установка для изображений правила `max-width: 100%` гарантирует, что они будут масштабироваться и не превышать ширину своего родительского контейнера, что является основой адаптивности.  Это предотвращает появление горизонтального скролла.  

# Вопрос 7. Зачем нужен `hover`-эффект для ссылок на веб-странице?
  
  
Варианты ответов:
1) Чтобы отключить заголовки
2) Чтобы убрать `href` у ссылок
3) ✅ Чтобы было понятно, что можно кликнуть
4) Чтобы подключить CSS дважды
5) Чтобы заменить список на таблицу
  
### ИИ анализ
Верный ответ.  `Hover`-эффект предоставляет пользователю визуальную подсказку (изменение цвета, подчеркивание), что элемент является интерактивным и на него можно нажать.  Это важный элемент юзабилити и доступности интерфейса.  

# Вопрос 8. Что такое “проверка адаптации” для веб-страницы?
  
  
Варианты ответов:
1) Переименовать `body` в `main`
2) Перенести CSS внутрь HTML
3) Удалить `doctype` и проверить
4) Удалить `link` на CSS файл
5) ✅ Проверить на узкой ширине окна
  
### ИИ анализ
Ответ правильный.  Проверка адаптации — это процесс тестирования вёрстки при разных размерах окна браузера, особенно на узких, имитирующих мобильные устройства.  Это позволяет убедиться, что контент остаётся читаемым и функциональным.  

# Вопрос 9. Что значит “глобальная правка дизайна” при финальной настройке страницы?
  
  
Варианты ответов:
1) Удалить все картинки из страницы
2) Удалить все секции из `main`
3) ✅ Поменять один общий параметр стиля
4) Убрать все классы из HTML
5) Удалить все ссылки из `footer`
  
### ИИ анализ
Выбран верный вариант.  Глобальная правка — это изменение базового параметра, такого как цветовая схема, шрифт или фон, который применяется ко многим элементам сразу (например, через селектор `body`).  Это эффективный способ быстрого обновления внешнего вида.  

Вопросы с текстовыми ответами: 11/22%
