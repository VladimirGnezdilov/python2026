Дней на выполнение: 53

result: 100/100

1) **Сильные стороны**
- Все функции объявлены корректно, соответствуют требованиям по именам и параметрам.
- В `format_message` корректно обрабатывается значение по умолчанию `tags=None` и проверка `tags is not None`, что предотвращает накопление тегов между вызовами.
- В `calculate` корректно обрабатывается пустой `args` и все три операции (`sum`, `mean`, `max`) реализованы через циклы, как требуется.
- `update_profile` просто возвращает `kwargs`, что полностью соответствует задаче.
- В задачах на область видимости объявлена глобальная переменная `total_energy` и две функции реализованы строго по условию: `add_energy_v1` использует `global`, `add_energy_v2` возвращает результат.
- Код чистый, читаемый, без лишних конструкций, соблюдены базовые правила PEP8 (пробелы, имена).

2) **Ошибки и недочёты (сгруппировать по серьёзности)**

**Блокирующие (ломает выполнение требований задания)**
- Отсутствуют.

**Значимые (может дать неверный результат на части кейсов, сильно ухудшает качество)**
- Отсутствуют.

**Минорные (стиль, читаемость, мелкие улучшения без влияния на правильность)**
- В функции `calculate` для операции `"mean"` результат деления возвращается как `float` (это хорошо), но в условии сказано "вернуть `float`". В текущей реализации при целых `args` в Python 3 результат будет `float` из-за оператора `/`. Это корректно. Можно отметить, что для явности можно использовать `float(total) / len(args)`, но это не требуется.
- В функции `calculate` при операции `"max"` и пустом `args` (который уже обработан ранее) ошибки не возникнет, так как пустой `args` приводит к возврату 0. Это корректно.
- Отсутствует демонстрационный код для вызова функций и проверки (как в псевдокоде условия для задачи 4). Однако в условии не было явного требования включать тестовые вызовы в решение, только описать функции. Поэтому это не является недочётом.

3) **Оценка и как она посчитана**
- Функциональность и соответствие условию: 50/50. Все четыре задачи реализованы полностью в соответствии с критериями. Функции принимают правильные параметры, обрабатывают граничные случаи (пустой `args`, `tags=None`), возвращают корректные результаты.
- Качество кода: 30/30. Код хорошо структурирован, функции независимы, нет дублирования (хотя в `calculate` сумма считается дважды для `"sum"` и `"mean"`, но это допустимо в учебных целях). Использованы понятные имена, нет глобальных побочных эффектов кроме явно оговоренного случая.
- Стиль и тесты: 20/20. Стиль соответствует PEP8 (отступы, пробелы, имена). Тестовый код не требовался, поэтому отсутствие тестов не штрафуется.

Итог: 100 баллов.

4) **Если задание выполнено не полностью**
- Задание выполнено полностью. Все требования учтены.
