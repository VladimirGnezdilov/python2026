# Домашнее задание. Урок 31. Словари и множества. паттерны использования, типичные ошибки

## Студент: Vladimir

По результатам урока у тебя хорошие решения по мутации словарей и множествам. ДЗ нацелено на закрепление и более осмысленный вывод.

## Задача 1. Сводка просмотров видео

Пример входных данных:
```python
views = [
    ("intro", 120),
    ("dicts", 300),
    ("sets", 180),
    ("dicts", 150),
]
```

### Требуется
1. Собрать словарь `seconds_by_lesson`.
2. Вывести строки вида `урок -> секунды`.

---

## Задача 2. Очистка неактивных пользователей

Пример входных данных:
```python
sessions = [
    ("user1", 3),
    ("user2", 1),
    ("user1", 2),
]
remove = ["user2", "bot"]
```

### Требуется
1. Собрать словарь `session_count`.
2. Удалить всех пользователей из `remove` через `pop(name, 0)`.
3. Напечатать итоговый словарь.

---

## Задача 3. Множества доменов

Пример входных данных:
```python
emails_a = {"a@corp.ru", "b@corp.ru", "c@gmail.com"}
emails_b = {"b@corp.ru", "d@corp.ru"}
```

### Требуется
1. Собрать множества доменов для `emails_a` и `emails_b`.
2. Найти домены, которые есть в обоих множествах.
3. Найти домены, которые есть только во втором множестве.

---

## Задача 4. Подготовка к comprehensions

Пример входных данных:
```python
errors = [
    ("dict", 5),
    ("set", 1),
    ("dict", 2),
]
limit = 5
```

### Требуется
1. Собрать словарь `error_counts`.
2. Построить новый словарь только для тем с количеством ошибок не меньше `limit`.
3. Напечатать оба словаря.
