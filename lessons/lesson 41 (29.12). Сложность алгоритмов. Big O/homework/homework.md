## Домашнее задание: определить **временную** сложность (Big O)

### Правила
Для каждого фрагмента кода:
- **Укажи Big O по времени** (в худшем случае).
- **Коротко объясни почему** (2-4 предложения, через количество шагов/итераций).
- **Обозначь, что такое `n`** (размер входа).

---

## Фрагмент A (один проход)
```python
def min_max(nums):
    mn = float("inf")
    mx = float("-inf")
    for x in nums:
        if x < mn:
            mn = x
        if x > mx:
            mx = x
    return mn, mx
```

---

## Фрагмент B (все пары)
```python
def count_pairs(nums):
    count = 0
    n = len(nums)
    for i in range(n):
        for j in range(i + 1, n):
            if nums[i] + nums[j] == 0:
                count += 1
    return count
```

---

## Фрагмент C (сортировка + проход)
```python
def has_duplicate(nums):
    nums = sorted(nums)
    for i in range(1, len(nums)):
        if nums[i] == nums[i - 1]:
            return True
    return False
```

---

## Что сдавать
- **Файл `homework_big_o.md`** (или ответ в LMS), структура:
  - A: `O(?)` + короткое пояснение
  - B: `O(?)` + короткое пояснение
  - C: `O(?)` + короткое пояснение

---

## Критерии оценки
- **3/3**: все 3 сложности верны и есть нормальное объяснение.
- **2/3**: все (или почти все) верно, но объяснения слабые.
- **1/3**: есть попытка рассуждать, но ответы в основном неверные.
- **0/3**: просто ответы без объяснения или совсем мимо.
